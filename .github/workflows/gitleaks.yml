name: gitleaks

on:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  pull-requests: read  # PR 스캔에 필요

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: "--verbose --redact"
